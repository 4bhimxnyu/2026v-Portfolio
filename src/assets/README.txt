Profile photo for the Contact card:
- avatar.jpg / .png / .webp: the main photo, shown whole (never cropped). The card is 9:16 (width:height), so a 9:16 portrait fills it exactly.
- avatar-mini.jpg / .png / .webp (optional): a square face crop for the small round avatar.
Both are published to public/images/ automatically before dev and build.
